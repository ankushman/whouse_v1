#!/usr/bin/env python3
"""R261: Generate Demand Sensing AI + Returns Prediction Engine modules."""
import os, re

def w(path, content):
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, 'w') as f:
        f.write(content)

BASE = "/home/z/my-project/src/components/modules"

# ═══════════════════════════════════════════════════════════════════════════════
# MODULE 1: Demand Sensing AI (demand-sensing-ai)
# 4 tabs: Dashboard | Demand Signals | Forecasts | Model Performance
# Theme: Blue #3b82f6 + Violet #7c3aed + Emerald #059669 + Amber #d97706
# ═══════════════════════════════════════════════════════════════════════════════

lines_a = []
L = lines_a.append

L('"use client"')
L('import { useState, useMemo } from "react"')
L('import { AreaChart, Area, BarChart, Bar, LineChart, Line, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip } from "recharts"')
L('import { BrainCircuit, TrendingUp, TrendingDown, Activity, BarChart3, Target, Zap, Signal, Search, ArrowUpDown, ChevronRight, Sparkles, AlertTriangle, CheckCircle2, Clock, Lightbulb } from "lucide-react"')
L('import { PageHeader } from "@/components/shared/page-header"')
L('import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"')
L('import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"')
L('import { Sheet, SheetContent, SheetHeader, SheetTitle } from "@/components/ui/sheet"')
L('import { Input } from "@/components/ui/input"')
L('')
L('// ── Constants ──')
L('const SIGNAL_TYPES = ["social_media", "weather", "event", "economic", "competitor", "search_trend", "supplier", "seasonal"] as const')
L('const SIGNAL_EMOJI: Record<string, string> = { social_media: "\\U0001f4f1", weather: "\\u2600\\ufe0f", event: "\\U0001f389", economic: "\\U0001f4b0", competitor: "\\U0001f3af", search_trend: "\\U0001f50d", supplier: "\\U0001f4e6", seasonal: "\\U0001f33f" }')
L('const CATEGORIES = ["electronics", "fashion", "grocery", "pharma", "auto", "furniture", "beauty", "sports"] as const')
L('const CAT_EMOJI: Record<string, string> = { electronics: "\\U0001f4bb", fashion: "\\U0001f457", grocery: "\\U0001f34e", pharma: "\\U0001f48a", auto: "\\U0001f697", furniture: "\\U0001f6cb", beauty: "\\U0001f9f5", sports: "\\u26bd" }')
L('const MODEL_STATUS = ["active", "training", "stale", "retired"] as const')
L('const CITIES = ["Mumbai", "Delhi", "Bangalore", "Chennai", "Hyderabad", "Kolkata", "Pune", "Ahmedabad"] as const')
L('const REGIONS = ["West", "North", "South", "East", "Central"] as const')
L('const MO = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"] as const')
L('const TH = { pri: "#3b82f6", sec: "#7c3aed", ok: "#059669", warn: "#d97706", err: "#dc2626" }')
L('const PC = ["#3b82f6", "#7c3aed", "#059669", "#d97706", "#dc2626", "#06b6d4", "#8b5cf6", "#ec4899"]')
L('')
L('// ── Utilities ──')
L('function seededRandom(seed: number): number { const x = Math.sin(seed * 9301 + 49297) * 233280; return x - Math.floor(x) }')
L('function ri(min: number, max: number, seed: number): number { return Math.floor(seededRandom(seed) * (max - min + 1)) + min }')
L('function pick<T>(arr: readonly T[], seed: number): T { return arr[Math.floor(seededRandom(seed) * arr.length)] }')
L('function filterData<T extends Record<string, unknown>>(d: T[], q: string): T[] { if (!q) return d; const lq = q.toLowerCase(); return d.filter(r => Object.values(r).some(v => typeof v === "string" && v.toLowerCase().includes(lq))) }')
L('function sortedData<T extends Record<string, unknown>>(d: T[], f: string, dir: "asc" | "desc"): T[] { return [...d].sort((a, b) => { const va = a[f], vb = b[f]; if (va == null || vb == null) return 0; const cmp = String(va).localeCompare(String(vb), undefined, { numeric: true }); return dir === "asc" ? cmp : -cmp }) }')
L('')
L('// ── Visual Components ──')
L('function SignalTypeBadge({ type }: { type: string }) {')
L('  const cols: Record<string, string> = { social_media: "bg-blue-100 text-blue-700 dark:bg-blue-900/30", weather: "bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30", event: "bg-amber-100 text-amber-700 dark:bg-amber-900/30", economic: "bg-violet-100 text-violet-700 dark:bg-violet-900/30", competitor: "bg-red-100 text-red-700 dark:bg-red-900/30", search_trend: "bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30", supplier: "bg-orange-100 text-orange-700 dark:bg-orange-900/30", seasonal: "bg-pink-100 text-pink-700 dark:bg-pink-900/30" }')
L('  return <span className={"dsa-signal-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (cols[type] || "bg-gray-100 text-gray-700")}>{SIGNAL_EMOJI[type] || "\\u2022"} {type.replace(/_/g, " ")}</span>')
L('}')
L('')
L('function CatBadge({ cat }: { cat: string }) {')
L('  return <span className={"dsa-cat-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold bg-violet-100 text-violet-700 dark:bg-violet-900/30"}>{CAT_EMOJI[cat] || "\\u2022"} {cat}</span>')
L('}')
L('')
L('function ModelStatusBadge({ status }: { status: string }) {')
L('  const cols: Record<string, string> = { active: "dsa-active-badge bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30", training: "dsa-training-badge bg-blue-100 text-blue-700 dark:bg-blue-900/30 animate-pulse", stale: "dsa-stale-badge bg-amber-100 text-amber-700 dark:bg-amber-900/30", retired: "dsa-retired-badge bg-gray-100 text-gray-500 dark:bg-gray-900/30" }')
L('  const icons: Record<string, React.ReactNode> = { active: <CheckCircle2 className="w-3 h-3" />, training: <Sparkles className="w-3 h-3" />, stale: <AlertTriangle className="w-3 h-3" />, retired: <Clock className="w-3 h-3" /> }')
L('  return <span className={"dsa-model-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (cols[status] || "")}>{icons[status]} {status}</span>')
L('}')
L('')
L('function ConfidenceGauge({ value }: { value: number }) {')
L('  const col = value >= 85 ? TH.ok : value >= 60 ? TH.warn : TH.err')
L('  return <div className="dsa-confidence-gauge flex items-center gap-1.5"><div className="w-16 h-2 rounded-full bg-gray-200 dark:bg-gray-700 overflow-hidden"><div className="h-full rounded-full transition-all" style={{ width: value + "%", backgroundColor: col }}/></div><span className="text-[10px] font-bold" style={{ color: col }}>{value}%</span></div>')
L('}')
L('')
L('function TrendIndicator({ value }: { value: number }) {')
L('  const pos = value > 0; const col = pos ? TH.ok : TH.err')
L('  return <span className="dsa-trend-indicator inline-flex items-center gap-0.5 text-[10px] font-semibold" style={{ color: col }}>{pos ? <TrendingUp className="w-3 h-3" /> : <TrendingDown className="w-3 h-3" />}{Math.abs(value).toFixed(1)}%</span>')
L('}')
L('')
L('function CityBadge({ city }: { city: string }) {')
L('  return <span className="dsa-city-badge inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-blue-50 text-blue-700 dark:bg-blue-900/20">{city}</span>')
L('}')
L('')
L('function RegionBadge({ region }: { region: string }) {')
L('  return <span className="dsa-region-badge inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-violet-50 text-violet-700 dark:bg-violet-900/20">{region}</span>')
L('}')
L('')
L('function AccuracyTile({ label, value }: { label: string; value: number }) {')
L('  const col = value >= 90 ? TH.ok : value >= 75 ? TH.warn : TH.err')
L('  return <div className="dsa-accuracy-tile text-center p-2 rounded-lg border" style={{ borderColor: col + "40" }}><div className="text-lg font-bold" style={{ color: col }}>{value}%</div><div className="text-[10px] text-muted-foreground">{label}</div></div>')
L('}')
L('')
L('function ImpactBadge({ impact }: { impact: string }) {')
L('  const cols: Record<string, string> = { high: "dsa-impact-high bg-red-100 text-red-700 dark:bg-red-900/30 shadow-[0_0_6px_rgba(220,38,38,0.3)]", medium: "dsa-impact-med bg-amber-100 text-amber-700 dark:bg-amber-900/30", low: "dsa-impact-low bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30" }')
L('  return <span className={"dsa-impact-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (cols[impact] || "")}>{impact}</span>')
L('}')
L('')
L('function ValueTile({ label, value, icon, trend }: { label: string; value: string; icon: React.ReactNode; trend: number }) {')
L('  return <Card className="dsa-value-tile glass-subtle hover:shadow-lg transition-shadow"><CardContent className="p-3"><div className="flex items-center justify-between"><span className="text-[10px] text-muted-foreground">{label}</span>{icon}</div><div className="text-xl font-bold mt-1">{value}</div><TrendIndicator value={trend}/></CardContent></Card>')
L('}')
L('')
L('function MAPEBar({ value }: { value: number }) {')
L('  const col = value <= 5 ? TH.ok : value <= 10 ? TH.warn : TH.err')
L('  return <div className="dsa-mape-bar flex items-center gap-1.5"><div className="w-12 h-2 rounded-full bg-gray-200 dark:bg-gray-700 overflow-hidden"><div className="h-full rounded-full" style={{ width: Math.min(value * 10, 100) + "%", backgroundColor: col }}/></div><span className="text-[10px] font-bold" style={{ color: col }}>{value}%</span></div>')
L('}')
L('')
L('function LatencyBadge({ ms }: { ms: number }) {')
L('  const col = ms <= 50 ? TH.ok : ms <= 200 ? TH.warn : TH.err')
L('  return <span className="dsa-latency-badge inline-flex items-center gap-1 text-[10px] font-bold" style={{ color: col }}><Zap className="w-3 h-3"/>{ms}ms</span>')
L('}')
L('')
L('function DriftIndicator({ drift }: { drift: number }) {')
L('  const col = drift <= 2 ? TH.ok : drift <= 5 ? TH.warn : TH.err')
L('  return <span className={"dsa-drift-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (drift <= 2 ? "bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30" : drift <= 5 ? "bg-amber-100 text-amber-700 dark:bg-amber-900/30" : "dsa-drift-critical bg-red-100 text-red-700 dark:bg-red-900/30 shadow-[0_0_8px_rgba(220,38,38,0.4)]")}><Activity className="w-3 h-3"/>Drift {drift}%</span>')
L('}')
L('')
L('// ── Data Generators ──')
L('function genSignals() {')
L('  return Array.from({ length: 80 }, (_, i) => {')
L('    const st = pick(SIGNAL_TYPES, i * 3 + 1)')
L('    const cat = pick(CATEGORIES, i * 3 + 2)')
L('    return {')
L('      id: "SIG-" + String(i + 1).padStart(4, "0"),')
L('      type: st,')
L('      category: cat,')
L('      city: pick(CITIES, i * 3 + 3),')
L('      region: pick(REGIONS, i * 3 + 4),')
L('      strength: ri(10, 100, i + 7),')
L('      impact: pick(["high", "medium", "low"] as const, i + 11),')
L('      confidence: ri(45, 98, i + 13),')
L('      trend: pick(["rising", "falling", "stable", "spike"] as const, i + 17),')
L('      source: pick(["Twitter", "News API", "WeatherStation", "MarketData", "Google Trends", "Internal ERP", "Supplier Feed"], i + 19),')
L('      timestamp: `2026-07-${String(ri(1, 30, i + 23)).padStart(2, "0")} ${String(ri(0, 23, i + 29)).padStart(2, "0")}:${String(ri(0, 59, i + 31)).padStart(2, "0")}`')
L('    }')
L('  })')
L('}')
L('')
L('function genForecasts() {')
L('  return Array.from({ length: 70 }, (_, i) => {')
L('    const cat = pick(CATEGORIES, i * 3 + 1)')
L('    return {')
L('      id: "FC-" + String(i + 1).padStart(4, "0"),')
L('      category: cat,')
L('      city: pick(CITIES, i * 3 + 2),')
L('      sku: "SKU-" + String(ri(10000, 99999, i + 7)).padStart(5, "0"),')
L('      current: ri(100, 5000, i + 13),')
L('      predicted: ri(100, 6000, i + 17),')
L('      variance: ri(-25, 30, i + 23),')
L('      confidence: ri(55, 97, i + 29),')
L('      horizon: pick(["7d", "14d", "30d", "60d", "90d"] as const, i + 31),')
L('      model: pick(["LSTM-v3", "Prophet-X", "XGBoost-AI", "Transformer", "ARIMA-Plus", "Ensemble-Hybrid"], i + 37),')
L('      lastUpdated: `2026-07-${String(ri(1, 30, i + 41)).padStart(2, "0")}`')
L('    }')
L('  })')
L('}')
L('')
L('function genModels() {')
L('  return Array.from({ length: 12 }, (_, i) => {')
L('    const st = pick(MODEL_STATUS, i * 3 + 1)')
L('    return {')
L('      id: "MDL-" + String(i + 1).padStart(3, "0"),')
L('      name: pick(["LSTM-v3", "Prophet-X", "XGBoost-AI", "Transformer-T", "ARIMA-Plus", "Ensemble-Hybrid", "LightGBM-Fast", "DeepAR-NET", "NBEATS-V2", "Temporal-Fusion", "CNN-LSTM", "WaveNet-Small"], i + 3),')
L('      status: st,')
L('      category: pick(CATEGORIES, i + 7),')
L('      mape: ri(2, 18, i + 11),')
L('      accuracy: ri(68, 98, i + 13),')
L('      latency: ri(12, 350, i + 17),')
L('      drift: ri(0, 12, i + 19),')
L('      trainSamples: ri(10000, 500000, i + 23),')
L('      lastTrained: `2026-07-${String(ri(1, 30, i + 29)).padStart(2, "0")}`,')
L('      version: `v${ri(1, 5, i + 31)}.${ri(0, 9, i + 37)}.${ri(0, 20, i + 41)}`')
L('    }')
L('  })')
L('}')
L('')
L('function genCharts() {')
L('  const monthly = MO.map((m, i) => ({ month: m, actual: ri(800, 3200, i + 101), predicted: ri(800, 3200, i + 151), signal_count: ri(20, 120, i + 201), alerts: ri(2, 25, i + 251) }))')
L('  const catPie = CATEGORIES.map((c, i) => ({ name: c, value: ri(500, 8000, i + 301) }))')
L('  const regionBar = REGIONS.map((r, i) => ({ region: r, demand: ri(2000, 15000, i + 401), fulfilled: ri(1500, 14000, i + 451), gap: ri(100, 2000, i + 501) }))')
L('  const accuracyLine = MO.map((m, i) => ({ month: m, mape: +(ri(3, 15, i + 551) / 10).toFixed(1), bias: +(ri(-8, 8, i + 601) / 10).toFixed(1) }))')
L('  return { monthly, catPie, regionBar, accuracyLine }')
L('}')
L('')
L('// ── Main Component ──')
L('export default function DemandSensingAiView() {')
L('  const [tab, setTab] = useState("dashboard")')
L('  const [search, setSearch] = useState("")')
L('  const [sortField, setSortField] = useState("id")')
L('  const [sortDir, setSortDir] = useState<"asc" | "desc">("asc")')
L('  const [detail, setDetail] = useState<Record<string, unknown> | null>(null)')
L('')
L('  const signals = useMemo(() => genSignals(), [])')
L('  const forecasts = useMemo(() => genForecasts(), [])')
L('  const models = useMemo(() => genModels(), [])')
L('  const charts = useMemo(() => genCharts(), [])')
L('  const filtSignals = useMemo(() => sortedData(filterData(signals, search), sortField, sortDir), [signals, search, sortField, sortDir])')
L('  const filtForecasts = useMemo(() => sortedData(filterData(forecasts, search), sortField, sortDir), [forecasts, search, sortField, sortDir])')
L('')
L('  const toggleSort = (f: string) => { if (sortField === f) setSortDir(d => d === "asc" ? "desc" : "asc"); else { setSortField(f); setSortDir("asc") } }')
L('  const sortIcon = (f: string) => sortField === f ? (sortDir === "asc" ? "\\u2191" : "\\u2193") : ""')
L('')
L('  const avgConf = Math.round(models.filter(m => m.status === "active").reduce((s, m) => s + m.accuracy, 0) / Math.max(1, models.filter(m => m.status === "active").length))')
L('  const activeModels = models.filter(m => m.status === "active").length')
L('  const totalSignals = signals.length')
L('  const highImpact = signals.filter(s => s.impact === "high").length')
L('')
L('  const tab0 = (')
L('    <div className="space-y-4">')
L('      <div className="grid grid-cols-2 md:grid-cols-4 gap-3">')
L('        <ValueTile label="Total Signals" value={totalSignals.toString()} icon={<Signal className="w-4 h-4 text-blue-500"/>} trend={8.5}/>')
L('        <ValueTile label="Active Models" value={activeModels.toString()} icon={<BrainCircuit className="w-4 h-4 text-violet-500"/>} trend={12.3}/>')
L('        <ValueTile label="Avg Accuracy" value={avgConf + "%"} icon={<Target className="w-4 h-4 text-emerald-500"/>} trend={3.2}/>')
L('        <ValueTile label="High Impact" value={highImpact.toString()} icon={<AlertTriangle className="w-4 h-4 text-red-500"/>} trend={-5.1}/>')
L('      </div>')
L('      <div className="grid grid-cols-1 md:grid-cols-2 gap-3">')
L('        <Card className="dsa-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Demand vs Forecast (Monthly)</CardTitle></CardHeader><CardContent><AreaChart data={charts.monthly} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="month" tick={{ fontSize: 10 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Area type="monotone" dataKey="actual" stroke={TH.pri} fill={TH.pri} fillOpacity={0.2}/><Area type="monotone" dataKey="predicted" stroke={TH.sec} fill={TH.sec} fillOpacity={0.15}/></AreaChart></CardContent></Card>')
L('        <Card className="dsa-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Demand by Category</CardTitle></CardHeader><CardContent><PieChart width={300} height={200}><Pie data={charts.catPie} cx="50%" cy="50%" outerRadius={70} dataKey="value" label={({ name, percent }) => name + " " + (percent * 100).toFixed(0) + "%"} labelLine={false}>{charts.catPie.map((_, i) => <Cell key={i} fill={PC[i % PC.length]}/>)}</Pie><Tooltip contentStyle={{ fontSize: 10 }}/></PieChart></CardContent></Card>')
L('        <Card className="dsa-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Regional Demand Gap</CardTitle></CardHeader><CardContent><BarChart data={charts.regionBar} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="region" tick={{ fontSize: 10 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Bar dataKey="demand" fill={TH.pri} radius={[2, 2, 0, 0]}/><Bar dataKey="fulfilled" fill={TH.ok} radius={[2, 2, 0, 0]}/><Bar dataKey="gap" fill={TH.warn} radius={[2, 2, 0, 0]}/></BarChart></CardContent></Card>')
L('        <Card className="dsa-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">MAPE & Bias Trend</CardTitle></CardHeader><CardContent><LineChart data={charts.accuracyLine} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="month" tick={{ fontSize: 10 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Line type="monotone" dataKey="mape" stroke={TH.warn} strokeWidth={2} dot={{ r: 3 }}/><Line type="monotone" dataKey="bias" stroke={TH.err} strokeWidth={2} strokeDasharray="5 5" dot={{ r: 3 }}/></LineChart></CardContent></Card>')
L('      </div>')
L('    </div>')
L('  )')
L('')
L('  const tab1 = (')
L('    <div className="space-y-3">')
L('      <div className="flex items-center gap-2"><Search className="w-4 h-4 text-muted-foreground"/><Input placeholder="Search signals..." value={search} onChange={e => setSearch(e.target.value)} className="h-8 text-xs max-w-xs"/></div>')
L('      <div className="rounded-lg border overflow-auto max-h-[calc(100vh-280px)]">')
L('        <table className="dsa-table w-full text-xs"><thead className="bg-blue-50 dark:bg-blue-900/20 sticky top-0"><tr><th className="p-2 text-left cursor-pointer select-none" onClick={() => toggleSort("id")}>ID {sortIcon("id")}</th><th className="p-2 text-left">Type</th><th className="p-2 text-left">Category</th><th className="p-2 text-left">City</th><th className="p-2 text-left">Impact</th><th className="p-2 text-left">Confidence</th><th className="p-2 text-left">Trend</th><th className="p-2 text-left">Source</th><th className="p-2 text-left">Time</th></tr></thead><tbody>{filtSignals.map(s => <tr key={s.id as string} className="dsa-table-row border-t hover:bg-blue-50/50 dark:hover:bg-blue-900/10 cursor-pointer" onClick={() => setDetail(s)}><td className="p-2 font-mono">{s.id as string}</td><td className="p-2"><SignalTypeBadge type={s.type as string}/></td><td className="p-2"><CatBadge cat={s.category as string}/></td><td className="p-2"><CityBadge city={s.city as string}/></td><td className="p-2"><ImpactBadge impact={s.impact as string}/></td><td className="p-2"><ConfidenceGauge value={s.confidence as number}/></td><td className="p-2"><span className="dsa-trend-cell text-[10px] font-medium">{s.trend as string}</span></td><td className="p-2 text-[10px]">{s.source as string}</td><td className="p-2 text-[10px] text-muted-foreground">{s.timestamp as string}</td></tr>)}</tbody></table>')
L('      </div>')
L('      <div className="text-[10px] text-muted-foreground text-right">{filtSignals.length} signals</div>')
L('    </div>')
L('  )')
L('')
L('  const tab2 = (')
L('    <div className="space-y-3">')
L('      <div className="flex items-center gap-2"><Search className="w-4 h-4 text-muted-foreground"/><Input placeholder="Search forecasts..." value={search} onChange={e => setSearch(e.target.value)} className="h-8 text-xs max-w-xs"/></div>')
L('      <div className="rounded-lg border overflow-auto max-h-[calc(100vh-280px)]">')
L('        <table className="dsa-table w-full text-xs"><thead className="bg-violet-50 dark:bg-violet-900/20 sticky top-0"><tr><th className="p-2 text-left cursor-pointer select-none" onClick={() => toggleSort("id")}>ID {sortIcon("id")}</th><th className="p-2 text-left">Category</th><th className="p-2 text-left">City</th><th className="p-2 text-left">SKU</th><th className="p-2 text-right">Current</th><th className="p-2 text-right">Predicted</th><th className="p-2 text-right">Variance</th><th className="p-2 text-left">Confidence</th><th className="p-2 text-left">Model</th><th className="p-2 text-left">Horizon</th></tr></thead><tbody>{filtForecasts.map(f => <tr key={f.id as string} className="dsa-table-row border-t hover:bg-violet-50/50 dark:hover:bg-violet-900/10 cursor-pointer" onClick={() => setDetail(f)}><td className="p-2 font-mono">{f.id as string}</td><td className="p-2"><CatBadge cat={f.category as string}/></td><td className="p-2"><CityBadge city={f.city as string}/></td><td className="p-2 font-mono text-[10px]">{f.sku as string}</td><td className="p-2 text-right">{(f.current as number).toLocaleString()}</td><td className="p-2 text-right font-semibold">{(f.predicted as number).toLocaleString()}</td><td className="p-2 text-right"><TrendIndicator value={f.variance as number}/></td><td className="p-2"><ConfidenceGauge value={f.confidence as number}/></td><td className="p-2 text-[10px]">{f.model as string}</td><td className="p-2"><span className="dsa-horizon-badge inline-flex px-1.5 py-0.5 rounded text-[10px] bg-blue-50 text-blue-700 dark:bg-blue-900/20">{f.horizon as string}</span></td></tr>)}</tbody></table>')
L('      </div>')
L('      <div className="text-[10px] text-muted-foreground text-right">{filtForecasts.length} forecasts</div>')
L('    </div>')
L('  )')
L('')
L('  const tab3 = (')
L('    <div className="space-y-4">')
L('      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">')
L('        {models.map(m => (')
L('          <Card key={m.id} className="dsa-model-card glass-subtle hover:shadow-lg transition-shadow">')
L('            <CardContent className="p-3 space-y-2">')
L('              <div className="flex items-center justify-between"><span className="font-semibold text-xs">{m.name}</span><ModelStatusBadge status={m.status}/></div>')
L('              <div className="grid grid-cols-2 gap-2">')
L('                <AccuracyTile label="MAPE" value={100 - m.mape}/>')
L('                <AccuracyTile label="Accuracy" value={m.accuracy}/>')
L('              </div>')
L('              <div className="flex items-center justify-between text-[10px]"><span className="text-muted-foreground">Latency</span><LatencyBadge ms={m.latency}/></div>')
L('              <div className="flex items-center justify-between text-[10px]"><span className="text-muted-foreground">Data Drift</span><DriftIndicator drift={m.drift}/></div>')
L('              <div className="text-[10px] text-muted-foreground">Training: {m.trainSamples.toLocaleString()} samples</div>')
L('              <div className="flex items-center justify-between text-[10px]"><span>{m.version}</span><span>{m.lastTrained}</span></div>')
L('            </CardContent>')
L('          </Card>')
L('        ))}')
L('      </div>')
L('    </div>')
L('  )')
L('')
L('  const tabs = [')
L('    { key: "dashboard", label: "Dashboard", icon: <BarChart3 className="w-3.5 h-3.5" />, content: tab0 },')
L('    { key: "signals", label: "Demand Signals", icon: <Signal className="w-3.5 h-3.5" />, content: tab1 },')
L('    { key: "forecasts", label: "Forecasts", icon: <TrendingUp className="w-3.5 h-3.5" />, content: tab2 },')
L('    { key: "models", label: "Model Performance", icon: <BrainCircuit className="w-3.5 h-3.5" />, content: tab3 }')
L('  ]')
L('')
L('  return (')
L('    <div className="space-y-4 p-4">')
L('      <PageHeader title="Demand Sensing AI" description="AI-powered demand sensing with real-time signal processing, multi-model forecasting, and drift monitoring"/>')
L('      <div className="flex items-center gap-3 flex-wrap">')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-blue-100 dark:bg-blue-900/30"><Sparkles className="w-3 h-3 text-blue-600"/><span className="text-[10px] font-semibold text-blue-700 dark:text-blue-300">{activeModels} AI Models Active</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-emerald-100 dark:bg-emerald-900/30"><CheckCircle2 className="w-3 h-3 text-emerald-600"/><span className="text-[10px] font-semibold text-emerald-700 dark:text-emerald-300">{avgConf}% Avg Accuracy</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-violet-100 dark:bg-violet-900/30"><Lightbulb className="w-3 h-3 text-violet-600"/><span className="text-[10px] font-semibold text-violet-700 dark:text-violet-300">{totalSignals} Signals Tracked</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-amber-100 dark:bg-amber-900/30"><AlertTriangle className="w-3 h-3 text-amber-600"/><span className="text-[10px] font-semibold text-amber-700 dark:text-amber-300">{highImpact} High Impact</span></div>')
L('      </div>')
L('      <Tabs value={tab} onValueChange={setTab}>')
L('        <TabsList className="bg-gradient-to-r from-blue-500/10 to-violet-500/10 p-0.5 h-9">')
L('          {tabs.map(t => <TabsTrigger key={t.key} value={t.key} className="text-xs gap-1.5 data-[state=active]:bg-blue-600 data-[state=active]:text-white">{t.icon}{t.label}</TabsTrigger>)}')
L('        </TabsList>')
L('        {tabs.map(t => tab === t.key && <div key={t.key} className="mt-3">{t.content}</div>)}')
L('      </Tabs>')
L('      <Sheet open={!!detail} onOpenChange={() => setDetail(null)}>')
L('        <SheetContent className="w-[420px] overflow-y-auto">')
L('          <SheetHeader><SheetTitle className="text-sm">Detail View</SheetTitle></SheetHeader>')
L('          {detail && <div className="mt-4 space-y-3">')
L('            <div className="dsa-detail-header rounded-lg p-4 bg-gradient-to-br from-blue-500 to-violet-600 text-white"><div className="text-lg font-bold">{detail.id}</div><div className="text-xs opacity-80 mt-1">{(detail.type || detail.category || detail.model || "Record")}</div></div>')
L('            {Object.entries(detail).filter(([k]) => k !== "id").map(([k, v]) => <div key={k} className="flex items-center justify-between py-1.5 border-b"><span className="text-[10px] text-muted-foreground capitalize">{k.replace(/_/g, " ")}</span><span className="text-xs font-medium">{typeof v === "number" ? v.toLocaleString() : String(v)}</span></div>)}')
L('          </div>}')
L('        </SheetContent>')
L('      </Sheet>')
L('    </div>')
L('  )')
L('}')
L('')

w(f"{BASE}/demand-sensing-ai-view.tsx", "\n".join(lines_a))
print(f"✅ demand-sensing-ai-view.tsx: {len(lines_a)} lines")


# ═══════════════════════════════════════════════════════════════════════════════
# MODULE 2: Returns Prediction Engine (returns-prediction-engine)
# 4 tabs: Dashboard | Return Predictions | Risk Analysis | Reduction Strategies
# Theme: Amber #d97706 + Red #dc2626 + Blue #3b82f6 + Emerald #059669
# ═══════════════════════════════════════════════════════════════════════════════

lines_b = []
L = lines_b.append

L('"use client"')
L('import { useState, useMemo } from "react"')
L('import { AreaChart, Area, BarChart, Bar, LineChart, Line, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip } from "recharts"')
L('import { Brain, TrendingUp, TrendingDown, ShieldAlert, ShieldCheck, Target, Search, ArrowUpDown, AlertTriangle, Package, Recycle, DollarSign, Clock, CheckCircle2, XCircle, Lightbulb, ArrowRight } from "lucide-react"')
L('import { PageHeader } from "@/components/shared/page-header"')
L('import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"')
L('import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"')
L('import { Sheet, SheetContent, SheetHeader, SheetTitle } from "@/components/ui/sheet"')
L('import { Input } from "@/components/ui/input"')
L('')
L('// ── Constants ──')
L('const RETURN_REASONS = ["defective", "wrong_item", "size_issue", "color_mismatch", "damaged_shipping", "changed_mind", "late_delivery", "quality_below"] as const')
L('const REASON_EMOJI: Record<string, string> = { defective: "\\u274c", wrong_item: "\\U0001f504", size_issue: "\\U0001f4cf", color_mismatch: "\\U0001f3a8", damaged_shipping: "\\U0001f4e6", changed_mind: "\\U0001f914", late_delivery: "\\u23f0", quality_below: "\\u2622\\ufe0f" }')
L('const CATEGORIES = ["electronics", "fashion", "grocery", "pharma", "furniture", "beauty", "sports", "auto"] as const')
L('const CAT_EMOJI: Record<string, string> = { electronics: "\\U0001f4bb", fashion: "\\U0001f457", grocery: "\\U0001f34e", pharma: "\\U0001f48a", furniture: "\\U0001f6cb", beauty: "\\U0001f9f5", sports: "\\u26bd", auto: "\\U0001f697" }')
L('const RISK_LEVELS = ["low", "medium", "high", "critical"] as const')
L('const CITIES = ["Mumbai", "Delhi", "Bangalore", "Chennai", "Hyderabad", "Kolkata", "Pune", "Ahmedabad"] as const')
L('const MO = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"] as const')
L('const TH = { pri: "#d97706", sec: "#dc2626", ok: "#059669", info: "#3b82f6" }')
L('const PC = ["#d97706", "#dc2626", "#3b82f6", "#059669", "#7c3aed", "#06b6d4", "#ec4899", "#8b5cf6"]')
L('')
L('// ── Utilities ──')
L('function seededRandom(seed: number): number { const x = Math.sin(seed * 9301 + 49297) * 233280; return x - Math.floor(x) }')
L('function ri(min: number, max: number, seed: number): number { return Math.floor(seededRandom(seed) * (max - min + 1)) + min }')
L('function pick<T>(arr: readonly T[], seed: number): T { return arr[Math.floor(seededRandom(seed) * arr.length)] }')
L('function filterData<T extends Record<string, unknown>>(d: T[], q: string): T[] { if (!q) return d; const lq = q.toLowerCase(); return d.filter(r => Object.values(r).some(v => typeof v === "string" && v.toLowerCase().includes(lq))) }')
L('function sortedData<T extends Record<string, unknown>>(d: T[], f: string, dir: "asc" | "desc"): T[] { return [...d].sort((a, b) => { const va = a[f], vb = b[f]; if (va == null || vb == null) return 0; const cmp = String(va).localeCompare(String(vb), undefined, { numeric: true }); return dir === "asc" ? cmp : -cmp }) }')
L('')
L('// ── Visual Components ──')
L('function ReasonBadge({ reason }: { reason: string }) {')
L('  const cols: Record<string, string> = { defective: "bg-red-100 text-red-700 dark:bg-red-900/30", wrong_item: "bg-blue-100 text-blue-700 dark:bg-blue-900/30", size_issue: "bg-amber-100 text-amber-700 dark:bg-amber-900/30", color_mismatch: "bg-pink-100 text-pink-700 dark:bg-pink-900/30", damaged_shipping: "bg-orange-100 text-orange-700 dark:bg-orange-900/30", changed_mind: "bg-violet-100 text-violet-700 dark:bg-violet-900/30", late_delivery: "bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30", quality_below: "bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30" }')
L('  return <span className={"rpe-reason-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (cols[reason] || "bg-gray-100 text-gray-700")}>{REASON_EMOJI[reason] || "\\u2022"} {reason.replace(/_/g, " ")}</span>')
L('}')
L('')
L('function CatBadge({ cat }: { cat: string }) {')
L('  return <span className={"rpe-cat-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold bg-amber-100 text-amber-700 dark:bg-amber-900/30"}>{CAT_EMOJI[cat] || "\\u2022"} {cat}</span>')
L('}')
L('')
L('function RiskBadge({ risk }: { risk: string }) {')
L('  const cols: Record<string, string> = { low: "rpe-risk-low bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30", medium: "rpe-risk-med bg-amber-100 text-amber-700 dark:bg-amber-900/30", high: "rpe-risk-high bg-red-100 text-red-700 dark:bg-red-900/30 shadow-[0_0_6px_rgba(220,38,38,0.3)]", critical: "rpe-risk-critical bg-red-200 text-red-800 dark:bg-red-900/40 shadow-[0_0_10px_rgba(220,38,38,0.5)] animate-pulse" }')
L('  return <span className={"rpe-risk-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold " + (cols[risk] || "")}>{risk.toUpperCase()}</span>')
L('}')
L('')
L('function CityBadge({ city }: { city: string }) {')
L('  return <span className="rpe-city-badge inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-amber-50 text-amber-700 dark:bg-amber-900/20">{city}</span>')
L('}')
L('')
L('function ProbGauge({ value }: { value: number }) {')
L('  const col = value >= 70 ? TH.sec : value >= 40 ? TH.pri : TH.ok')
L('  return <div className="rpe-prob-gauge flex items-center gap-1.5"><div className="w-16 h-2 rounded-full bg-gray-200 dark:bg-gray-700 overflow-hidden"><div className="h-full rounded-full transition-all" style={{ width: value + "%", backgroundColor: col }}/></div><span className="text-[10px] font-bold" style={{ color: col }}>{value}%</span></div>')
L('}')
L('')
L('function CostBadge({ cost }: { cost: number }) {')
L('  const col = cost >= 5000 ? TH.sec : cost >= 2000 ? TH.pri : TH.ok')
L('  return <span className="rpe-cost-badge inline-flex items-center gap-1 text-[10px] font-bold" style={{ color: col }}><DollarSign className="w-3 h-3"/>{cost.toLocaleString()}</span>')
L('}')
L('')
L('function TrendIndicator({ value }: { value: number }) {')
L('  const neg = value < 0; const col = neg ? TH.sec : TH.ok')
L('  return <span className="rpe-trend-indicator inline-flex items-center gap-0.5 text-[10px] font-semibold" style={{ color: col }}>{neg ? <TrendingUp className="w-3 h-3" /> : <TrendingDown className="w-3 h-3" />}{Math.abs(value).toFixed(1)}%</span>')
L('}')
L('')
L('function ValueTile({ label, value, icon, trend }: { label: string; value: string; icon: React.ReactNode; trend: number }) {')
L('  return <Card className="rpe-value-tile glass-subtle hover:shadow-lg transition-shadow"><CardContent className="p-3"><div className="flex items-center justify-between"><span className="text-[10px] text-muted-foreground">{label}</span>{icon}</div><div className="text-xl font-bold mt-1">{value}</div><TrendIndicator value={trend}/></CardContent></Card>')
L('}')
L('')
L('function SavingsTile({ label, value }: { label: string; value: string }) {')
L('  return <div className="rpe-savings-tile p-3 rounded-lg border border-emerald-200 dark:border-emerald-800 bg-emerald-50 dark:bg-emerald-900/20 text-center"><div className="text-lg font-bold text-emerald-600">{value}</div><div className="text-[10px] text-muted-foreground">{label}</div></div>')
L('}')
L('')
L('function StrategyBadge({ status }: { status: string }) {')
L('  const cols: Record<string, string> = { active: "rpe-strategy-active bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30", planned: "rpe-strategy-plan bg-blue-100 text-blue-700 dark:bg-blue-900/30", piloting: "rpe-strategy-pilot bg-violet-100 text-violet-700 dark:bg-violet-900/30", completed: "rpe-strategy-done bg-gray-100 text-gray-600 dark:bg-gray-800" }')
L('  return <span className={"rpe-strategy-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-semibold " + (cols[status] || "")}>{status}</span>')
L('}')
L('')
L('function ProgressRing({ pct }: { pct: number }) {')
L('  const col = pct >= 80 ? TH.ok : pct >= 50 ? TH.info : TH.pri')
L('  const r = 18; const circ = 2 * Math.PI * r; const off = circ * (1 - pct / 100)')
L('  return <svg width={44} height={44} className="rpe-progress-ring"><circle cx={22} cy={22} r={r} fill="none" stroke="currentColor" strokeWidth={3} className="text-gray-200 dark:text-gray-700"/><circle cx={22} cy={22} r={r} fill="none" stroke={col} strokeWidth={3} strokeDasharray={circ} strokeDashoffset={off} strokeLinecap="round" transform="rotate(-90 22 22)"/><text x={22} y={22} textAnchor="middle" dominantBaseline="central" className="text-[9px] font-bold fill-current" style={{ color: col }}>{pct}%</text></svg>')
L('}')
L('')
L('function ROIIndicator({ roi }: { roi: number }) {')
L('  const col = roi >= 200 ? TH.ok : roi >= 100 ? TH.info : TH.pri')
L('  return <span className="rpe-roi-badge inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold" style={{ backgroundColor: col + "20", color: col }}>{roi}x ROI</span>')
L('}')
L('')
L('// ── Data Generators ──')
L('function genPredictions() {')
L('  return Array.from({ length: 80 }, (_, i) => {')
L('    const reason = pick(RETURN_REASONS, i * 3 + 1)')
L('    const cat = pick(CATEGORIES, i * 3 + 2)')
L('    return {')
L('      id: "RP-" + String(i + 1).padStart(4, "0"),')
L('      orderId: "ORD-" + String(ri(100000, 999999, i + 7)).padStart(6, "0"),')
L('      category: cat,')
L('      reason: reason,')
L('      city: pick(CITIES, i * 3 + 3),')
L('      returnProb: ri(5, 95, i + 11),')
L('      risk: pick(RISK_LEVELS, i + 13),')
L('      estimatedCost: ri(200, 15000, i + 17),')
L('      predictedDate: `2026-08-${String(ri(1, 31, i + 19)).padStart(2, "0")}`,')
L('      model: pick(["RandomForest", "XGBoost-Returns", "NeuralNet-V2", "LogisticReg", "Ensemble-Returns", "LightGBM-R"], i + 23),')
L('      confidence: ri(50, 97, i + 29),')
L('      customerSegment: pick(["Premium", "Regular", "First-time", "Wholesale", "Loyalty"], i + 31)')
L('    }')
L('  })')
L('}')
L('')
L('function genRisks() {')
L('  return Array.from({ length: 60 }, (_, i) => {')
L('    const cat = pick(CATEGORIES, i * 3 + 1)')
L('    return {')
L('      id: "RSK-" + String(i + 1).padStart(4, "0"),')
L('      category: cat,')
L('      city: pick(CITIES, i * 3 + 2),')
L('      riskFactor: pick(["supplier_quality", "packaging", "shipping_carrier", "last_mile", "customer_expectation", "product_complexity", "storage_conditions", "handling_procedures"], i + 3),')
L('      riskLevel: pick(RISK_LEVELS, i + 7),')
L('      returnRate: ri(2, 35, i + 11),')
L('      avgCostPerReturn: ri(300, 8000, i + 13),')
L('      monthlyImpact: ri(10, 500, i + 17),')
L('      trend: pick(["increasing", "decreasing", "stable", "spike"], i + 19),')
L('      mitigation: pick(["Improve packaging", "Switch carrier", "Better QC", "Enhanced inspection", "Customer education", "Improve product desc", "Better handling SOP"], i + 23)')
L('    }')
L('  })')
L('}')
L('')
L('function genStrategies() {')
L('  return Array.from({ length: 16 }, (_, i) => {')
L('    const st = pick(["active", "planned", "piloting", "completed"] as const, i * 2 + 1)')
L('    return {')
L('      id: "STR-" + String(i + 1).padStart(3, "0"),')
L('      name: pick(["Enhanced Pre-Ship QC", "AI Size Recommendation", "Virtual Try-On", "Improved Packaging", "Carrier Scorecard", "Customer Feedback Loop", "Predictive QA Gates", "Return Reason Dashboard", "Auto-Refund Threshold", "Restock Automation", "Product Photo Accuracy", "Dimensional Check AI", "Smart Return Routing", "Quality Correlation AI", "Supplier Penalty Program", "Last-Mile Protection"], i + 2),')
L('      status: st,')
L('      reductionPct: ri(5, 45, i + 7),')
L('      savings: ri(50000, 2000000, i + 11),')
L('      roi: ri(80, 400, i + 13),')
L('      progress: st === "completed" ? 100 : ri(10, 90, i + 17),')
L('      category: pick(CATEGORIES, i + 19),')
L('      startDate: `2026-${String(ri(1, 7, i + 23)).padStart(2, "0")}-${String(ri(1, 28, i + 29)).padStart(2, "0")}`')
L('    }')
L('  })')
L('}')
L('')
L('function genCharts() {')
L('  const monthly = MO.map((m, i) => ({ month: m, actual_returns: ri(200, 1200, i + 101), predicted_returns: ri(200, 1200, i + 151), prevented: ri(50, 400, i + 201), cost_savings: ri(100000, 800000, i + 251) }))')
L('  const reasonPie = RETURN_REASONS.map((r, i) => ({ name: r.replace(/_/g, " "), value: ri(50, 800, i + 301) }))')
L('  const catBar = CATEGORIES.map((c, i) => ({ category: c, return_rate: ri(3, 28, i + 401), cost: ri(50000, 500000, i + 451) }))')
L('  const riskLine = MO.map((m, i) => ({ month: m, avg_risk: +(ri(15, 55, i + 501) / 10).toFixed(1), high_risk_count: ri(5, 40, i + 551) }))')
L('  return { monthly, reasonPie, catBar, riskLine }')
L('}')
L('')
L('// ── Main Component ──')
L('export default function ReturnsPredictionEngineView() {')
L('  const [tab, setTab] = useState("dashboard")')
L('  const [search, setSearch] = useState("")')
L('  const [sortField, setSortField] = useState("id")')
L('  const [sortDir, setSortDir] = useState<"asc" | "desc">("asc")')
L('  const [detail, setDetail] = useState<Record<string, unknown> | null>(null)')
L('')
L('  const predictions = useMemo(() => genPredictions(), [])')
L('  const risks = useMemo(() => genRisks(), [])')
L('  const strategies = useMemo(() => genStrategies(), [])')
L('  const charts = useMemo(() => genCharts(), [])')
L('  const filtPreds = useMemo(() => sortedData(filterData(predictions, search), sortField, sortDir), [predictions, search, sortField, sortDir])')
L('  const filtRisks = useMemo(() => sortedData(filterData(risks, search), sortField, sortDir), [risks, search, sortField, sortDir])')
L('')
L('  const toggleSort = (f: string) => { if (sortField === f) setSortDir(d => d === "asc" ? "desc" : "asc"); else { setSortField(f); setSortDir("asc") } }')
L('  const sortIcon = (f: string) => sortField === f ? (sortDir === "asc" ? "\\u2191" : "\\u2193") : ""')
L('')
L('  const avgReturnRate = Math.round(risks.reduce((s, r) => s + r.returnRate, 0) / risks.length)')
L('  const totalCost = predictions.reduce((s, p) => s + p.estimatedCost, 0)')
L('  const highRiskCount = risks.filter(r => r.riskLevel === "high" || r.riskLevel === "critical").length')
L('  const activeStrategies = strategies.filter(s => s.status === "active" || s.status === "piloting").length')
L('')
L('  const tab0 = (')
L('    <div className="space-y-4">')
L('      <div className="grid grid-cols-2 md:grid-cols-4 gap-3">')
L('        <ValueTile label="Avg Return Rate" value={avgReturnRate + "%"} icon={<Package className="w-4 h-4 text-amber-500"/>} trend={-4.2}/>')
L('        <ValueTile label="Total Cost at Risk" value={"\\u20b9" + (totalCost / 100000).toFixed(1) + "L"} icon={<DollarSign className="w-4 h-4 text-red-500"/>} trend={6.8}/>')
L('        <ValueTile label="High Risk Items" value={highRiskCount.toString()} icon={<ShieldAlert className="w-4 h-4 text-red-500"/>} trend={2.1}/>')
L('        <ValueTile label="Active Strategies" value={activeStrategies.toString()} icon={<Lightbulb className="w-4 h-4 text-blue-500"/>} trend={15.5}/>')
L('      </div>')
L('      <div className="grid grid-cols-1 md:grid-cols-2 gap-3">')
L('        <Card className="rpe-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Returns Trend (Actual vs Predicted)</CardTitle></CardHeader><CardContent><AreaChart data={charts.monthly} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="month" tick={{ fontSize: 10 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Area type="monotone" dataKey="actual_returns" stroke={TH.pri} fill={TH.pri} fillOpacity={0.2}/><Area type="monotone" dataKey="predicted_returns" stroke={TH.sec} fill={TH.sec} fillOpacity={0.15}/></AreaChart></CardContent></Card>')
L('        <Card className="rpe-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Return Reasons Distribution</CardTitle></CardHeader><CardContent><PieChart width={300} height={200}><Pie data={charts.reasonPie} cx="50%" cy="50%" outerRadius={70} dataKey="value" label={({ name, percent }) => name + " " + (percent * 100).toFixed(0) + "%"} labelLine={false}>{charts.reasonPie.map((_, i) => <Cell key={i} fill={PC[i % PC.length]}/>)}</Pie><Tooltip contentStyle={{ fontSize: 10 }}/></PieChart></CardContent></Card>')
L('        <Card className="rpe-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Return Rate & Cost by Category</CardTitle></CardHeader><CardContent><BarChart data={charts.catBar} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="category" tick={{ fontSize: 9 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Bar dataKey="return_rate" fill={TH.pri} radius={[2, 2, 0, 0]}/><Bar dataKey="cost" fill={TH.sec} radius={[2, 2, 0, 0]}/></BarChart></CardContent></Card>')
L('        <Card className="rpe-chart-card glass-subtle"><CardHeader className="pb-1"><CardTitle className="text-xs">Risk Score Trend</CardTitle></CardHeader><CardContent><LineChart data={charts.riskLine} height={200}><CartesianGrid strokeDasharray="3 3" opacity={0.3}/><XAxis dataKey="month" tick={{ fontSize: 10 }}/><YAxis tick={{ fontSize: 10 }}/><Tooltip contentStyle={{ fontSize: 10 }}/><Line type="monotone" dataKey="avg_risk" stroke={TH.pri} strokeWidth={2} dot={{ r: 3 }}/><Line type="monotone" dataKey="high_risk_count" stroke={TH.sec} strokeWidth={2} strokeDasharray="5 5" dot={{ r: 3 }}/></LineChart></CardContent></Card>')
L('      </div>')
L('    </div>')
L('  )')
L('')
L('  const tab1 = (')
L('    <div className="space-y-3">')
L('      <div className="flex items-center gap-2"><Search className="w-4 h-4 text-muted-foreground"/><Input placeholder="Search predictions..." value={search} onChange={e => setSearch(e.target.value)} className="h-8 text-xs max-w-xs"/></div>')
L('      <div className="rounded-lg border overflow-auto max-h-[calc(100vh-280px)]">')
L('        <table className="rpe-table w-full text-xs"><thead className="bg-amber-50 dark:bg-amber-900/20 sticky top-0"><tr><th className="p-2 text-left cursor-pointer select-none" onClick={() => toggleSort("id")}>ID {sortIcon("id")}</th><th className="p-2 text-left">Order</th><th className="p-2 text-left">Category</th><th className="p-2 text-left">Reason</th><th className="p-2 text-left">City</th><th className="p-2 text-left">Return Prob</th><th className="p-2 text-left">Risk</th><th className="p-2 text-right">Est. Cost</th><th className="p-2 text-left">Segment</th><th className="p-2 text-left">Model</th></tr></thead><tbody>{filtPreds.map(p => <tr key={p.id as string} className="rpe-table-row border-t hover:bg-amber-50/50 dark:hover:bg-amber-900/10 cursor-pointer" onClick={() => setDetail(p)}><td className="p-2 font-mono">{p.id as string}</td><td className="p-2 font-mono text-[10px]">{p.orderId as string}</td><td className="p-2"><CatBadge cat={p.category as string}/></td><td className="p-2"><ReasonBadge reason={p.reason as string}/></td><td className="p-2"><CityBadge city={p.city as string}/></td><td className="p-2"><ProbGauge value={p.returnProb as number}/></td><td className="p-2"><RiskBadge risk={p.risk as string}/></td><td className="p-2 text-right"><CostBadge cost={p.estimatedCost as number}/></td><td className="p-2 text-[10px]">{p.customerSegment as string}</td><td className="p-2 text-[10px]">{p.model as string}</td></tr>)}</tbody></table>')
L('      </div>')
L('      <div className="text-[10px] text-muted-foreground text-right">{filtPreds.length} predictions</div>')
L('    </div>')
L('  )')
L('')
L('  const tab2 = (')
L('    <div className="space-y-3">')
L('      <div className="flex items-center gap-2"><Search className="w-4 h-4 text-muted-foreground"/><Input placeholder="Search risks..." value={search} onChange={e => setSearch(e.target.value)} className="h-8 text-xs max-w-xs"/></div>')
L('      <div className="rounded-lg border overflow-auto max-h-[calc(100vh-280px)]">')
L('        <table className="rpe-table w-full text-xs"><thead className="bg-red-50 dark:bg-red-900/20 sticky top-0"><tr><th className="p-2 text-left cursor-pointer select-none" onClick={() => toggleSort("id")}>ID {sortIcon("id")}</th><th className="p-2 text-left">Category</th><th className="p-2 text-left">City</th><th className="p-2 text-left">Risk Factor</th><th className="p-2 text-left">Level</th><th className="p-2 text-right">Return Rate</th><th className="p-2 text-right">Avg Cost</th><th className="p-2 text-right">Monthly Impact</th><th className="p-2 text-left">Trend</th><th className="p-2 text-left">Mitigation</th></tr></thead><tbody>{filtRisks.map(r => <tr key={r.id as string} className="rpe-table-row border-t hover:bg-red-50/50 dark:hover:bg-red-900/10 cursor-pointer" onClick={() => setDetail(r)}><td className="p-2 font-mono">{r.id as string}</td><td className="p-2"><CatBadge cat={r.category as string}/></td><td className="p-2"><CityBadge city={r.city as string}/></td><td className="p-2"><span className="rpe-risk-factor text-[10px] font-medium">{(r.riskFactor as string).replace(/_/g, " ")}</span></td><td className="p-2"><RiskBadge risk={r.riskLevel as string}/></td><td className="p-2 text-right font-semibold">{r.returnRate}%</td><td className="p-2 text-right"><CostBadge cost={r.avgCostPerReturn as number}/></td><td className="p-2 text-right">{r.monthlyImpact}</td><td className="p-2"><span className="rpe-trend-cell text-[10px]">{r.trend as string}</span></td><td className="p-2 text-[10px] max-w-[150px] truncate">{r.mitigation as string}</td></tr>)}</tbody></table>')
L('      </div>')
L('      <div className="text-[10px] text-muted-foreground text-right">{filtRisks.length} risk items</div>')
L('    </div>')
L('  )')
L('')
L('  const tab3 = (')
L('    <div className="space-y-4">')
L('      <div className="grid grid-cols-2 md:grid-cols-4 gap-3">')
L('        {strategies.filter(s => s.status === "active").slice(0, 4).map(s => <SavingsTile key={s.id} label={s.name} value={"\\u20b9" + (s.savings / 100000).toFixed(1) + "L"}/>).concat(strategies.filter(s => s.status !== "active").slice(0, 4).map(s => <SavingsTile key={s.id} label={s.name} value={"\\u20b9" + (s.savings / 100000).toFixed(1) + "L"/>))}')
L('      </div>')
L('      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">')
L('        {strategies.map(s => (')
L('          <Card key={s.id} className="rpe-strategy-card glass-subtle hover:shadow-lg transition-shadow">')
L('            <CardContent className="p-3 space-y-2">')
L('              <div className="flex items-center justify-between"><span className="font-semibold text-xs">{s.name}</span><StrategyBadge status={s.status}/></div>')
L('              <div className="flex items-center gap-4">')
L('                <ProgressRing pct={s.progress}/>')
L('                <div className="flex-1 space-y-1">')
L('                  <div className="flex items-center justify-between text-[10px]"><span className="text-muted-foreground">Reduction</span><span className="font-semibold">{s.reductionPct}%</span></div>')
L('                  <div className="flex items-center justify-between text-[10px]"><span className="text-muted-foreground">Savings</span><span className="font-semibold text-emerald-600">\\u20b9{(s.savings / 100000).toFixed(1)}L</span></div>')
L('                </div>')
L('              </div>')
L('              <div className="flex items-center justify-between"><ROIIndicator roi={s.roi}/><CatBadge cat={s.category}/></div>')
L('            </CardContent>')
L('          </Card>')
L('        ))}')
L('      </div>')
L('    </div>')
L('  )')
L('')
L('  const tabs = [')
L('    { key: "dashboard", label: "Dashboard", icon: <Target className="w-3.5 h-3.5" />, content: tab0 },')
L('    { key: "predictions", label: "Return Predictions", icon: <Brain className="w-3.5 h-3.5" />, content: tab1 },')
L('    { key: "risks", label: "Risk Analysis", icon: <ShieldAlert className="w-3.5 h-3.5" />, content: tab2 },')
L('    { key: "strategies", label: "Reduction Strategies", icon: <Lightbulb className="w-3.5 h-3.5" />, content: tab3 }')
L('  ]')
L('')
L('  return (')
L('    <div className="space-y-4 p-4">')
L('      <PageHeader title="Returns Prediction Engine" description="AI-powered return risk prediction with multi-factor analysis, cost modeling, and proactive reduction strategies"/>')
L('      <div className="flex items-center gap-3 flex-wrap">')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-amber-100 dark:bg-amber-900/30"><ShieldAlert className="w-3 h-3 text-amber-600"/><span className="text-[10px] font-semibold text-amber-700 dark:text-amber-300">{highRiskCount} High Risk</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-emerald-100 dark:bg-emerald-900/30"><ShieldCheck className="w-3 h-3 text-emerald-600"/><span className="text-[10px] font-semibold text-emerald-700 dark:text-emerald-300">{activeStrategies} Strategies Active</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-blue-100 dark:bg-blue-900/30"><Recycle className="w-3 h-3 text-blue-600"/><span className="text-[10px] font-semibold text-blue-700 dark:text-blue-300">{avgReturnRate}% Avg Return</span></div>')
L('        <div className="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-red-100 dark:bg-red-900/30"><DollarSign className="w-3 h-3 text-red-600"/><span className="text-[10px] font-semibold text-red-700 dark:text-red-300">\\u20b9{(totalCost/100000).toFixed(0)}L Cost at Risk</span></div>')
L('      </div>')
L('      <Tabs value={tab} onValueChange={setTab}>')
L('        <TabsList className="bg-gradient-to-r from-amber-500/10 to-red-500/10 p-0.5 h-9">')
L('          {tabs.map(t => <TabsTrigger key={t.key} value={t.key} className="text-xs gap-1.5 data-[state=active]:bg-amber-600 data-[state=active]:text-white">{t.icon}{t.label}</TabsTrigger>)}')
L('        </TabsList>')
L('        {tabs.map(t => tab === t.key && <div key={t.key} className="mt-3">{t.content}</div>)}')
L('      </Tabs>')
L('      <Sheet open={!!detail} onOpenChange={() => setDetail(null)}>')
L('        <SheetContent className="w-[420px] overflow-y-auto">')
L('          <SheetHeader><SheetTitle className="text-sm">Detail View</SheetTitle></SheetHeader>')
L('          {detail && <div className="mt-4 space-y-3">')
L('            <div className="rpe-detail-header rounded-lg p-4 bg-gradient-to-br from-amber-500 to-red-600 text-white"><div className="text-lg font-bold">{detail.id}</div><div className="text-xs opacity-80 mt-1">{(detail.category || detail.riskFactor || detail.reason || "Record")}</div></div>')
L('            {Object.entries(detail).filter(([k]) => k !== "id").map(([k, v]) => <div key={k} className="flex items-center justify-between py-1.5 border-b"><span className="text-[10px] text-muted-foreground capitalize">{k.replace(/_/g, " ")}</span><span className="text-xs font-medium">{typeof v === "number" ? v.toLocaleString() : String(v)}</span></div>)}')
L('          </div>}')
L('        </SheetContent>')
L('      </Sheet>')
L('    </div>')
L('  )')
L('}')
L('')

w(f"{BASE}/returns-prediction-engine-view.tsx", "\n".join(lines_b))
print(f"✅ returns-prediction-engine-view.tsx: {len(lines_b)} lines")
print(f"\nTotal new lines: {len(lines_a) + len(lines_b)}")
